# Lock window for auto-locking matches before kickoff.
# A match becomes locked LOCK_WINDOW_HOURS hours before its match_date (UTC).
# Default: 3 hours. Set to a small value (e.g. 0.01) for fast local testing.
LOCK_WINDOW_HOURS=3
